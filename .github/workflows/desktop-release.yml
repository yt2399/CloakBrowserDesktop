name: Desktop Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  attestations: write

concurrency:
  group: desktop-release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate-version:
    name: Validate version
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Check tag matches package version
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        run: |
          PACKAGE_VERSION="$(node -p "require('./package.json').version")"
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          echo "Tag: ${TAG_VERSION} | package.json: ${PACKAGE_VERSION}"
          test "${TAG_VERSION}" = "${PACKAGE_VERSION}"

      - name: Check release notes exist
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        run: |
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          RELEASE_NOTES="release-notes/${TAG_VERSION}.md"
          echo "Release notes: ${RELEASE_NOTES}"
          test -s "${RELEASE_NOTES}"
          grep -q '^## ' "${RELEASE_NOTES}"

  package-windows:
    name: Package Windows x64
    needs: validate-version
    runs-on: windows-latest
    timeout-minutes: 45
    env:
      ELECTRON_MIRROR: https://github.com/electron/electron/releases/download/
      CSC_LINK: ${{ secrets.WINDOWS_CSC_LINK }}
      CSC_KEY_PASSWORD: ${{ secrets.WINDOWS_CSC_KEY_PASSWORD }}
      WIN_CSC_LINK: ${{ secrets.WINDOWS_CSC_LINK }}
      WIN_CSC_KEY_PASSWORD: ${{ secrets.WINDOWS_CSC_KEY_PASSWORD }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Typecheck and bundle
        run: npm run build

      - name: Report Windows code signing mode
        shell: bash
        run: |
          if [ -n "${WIN_CSC_LINK:-}" ] || [ -n "${CSC_LINK:-}" ]; then
            echo "Windows code signing certificate secret is configured."
          else
            echo "Windows code signing certificate secret is not configured; installer will be unsigned."
          fi

      - name: Package application
        shell: bash
        run: |
          for attempt in 1 2 3; do
            npx electron-builder --win nsis --x64 --publish never && exit 0
            if [ "$attempt" -eq 3 ]; then
              exit 1
            fi
            echo "Packaging failed on attempt ${attempt}; retrying in 10 seconds..."
            sleep 10
          done

      - name: Attest installer provenance
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: |
            dist/*.exe
            dist/*.exe.blockmap
            dist/latest.yml

      - name: Upload installer artifact
        uses: actions/upload-artifact@v7
        with:
          name: cloakbrowser-desktop-windows-x64
          path: |
            dist/*.exe
            dist/*.exe.blockmap
            dist/latest.yml
          if-no-files-found: error
          retention-days: 14

  github-release:
    name: Publish GitHub Release
    if: startsWith(github.ref, 'refs/tags/')
    needs: package-windows
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Download installer artifact
        uses: actions/download-artifact@v8
        with:
          name: cloakbrowser-desktop-windows-x64
          path: release-assets

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          RELEASE_NOTES="release-notes/${GITHUB_REF_NAME#v}.md"
          gh release create "$GITHUB_REF_NAME" release-assets/* \
            --repo "$GITHUB_REPOSITORY" \
            --title "CloakBrowser Desktop ${GITHUB_REF_NAME#v}" \
            --notes-file "$RELEASE_NOTES"
